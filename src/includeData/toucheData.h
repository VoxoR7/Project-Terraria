strcpy( (configTouche + QUITTER)->descriptif, "quitter");
(configTouche + QUITTER)->keyCode = TOUCHE_NULL;

strcpy( (configTouche + ESCAPE)->descriptif, "escape");
(configTouche + ESCAPE)->keyCode = SDLK_ESCAPE;

strcpy( (configTouche + MONTER)->descriptif, "monter");
(configTouche + MONTER)->keyCode = SDLK_z;

strcpy( (configTouche + DESCENDRE)->descriptif, "descendre");
(configTouche + DESCENDRE)->keyCode = SDLK_s;

strcpy( (configTouche + DROITE)->descriptif, "droite");
(configTouche + DROITE)->keyCode = SDLK_d;

strcpy( (configTouche + GAUCHE)->descriptif, "gauche");
(configTouche + GAUCHE)->keyCode = SDLK_q;

strcpy( (configTouche + ESPACE)->descriptif, "espace");
(configTouche + ESPACE)->keyCode = SDLK_SPACE;